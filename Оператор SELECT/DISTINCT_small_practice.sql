-- Task 1: Select unique job_id from job_history table
SELECT DISTINCT job_id
FROM job_history;

-- Task 2: Select unique job_id and department_id combination from job_history table
SELECT DISTINCT job_id, department_id
FROM job_history;